from setuptools import setup, find_packages

setup(
    name="markpdfdown",
    version="1.0.4",
    packages=find_packages(),
    install_requires=[
        # 从 requirements.txt 中读取依赖
    ],
    python_requires=">=3.9",
) 