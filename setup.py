from setuptools import setup, find_packages

setup(
    name="markpdfdown",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # 从 requirements.txt 中读取依赖
    ],
    python_requires=">=3.9",
) 